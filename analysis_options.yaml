include: package:very_good_analysis/analysis_options.yaml

formatter:
  page_width: 100

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

  exclude:
    # Generated code
    - test/.test_coverage.dart
    - lib/generated_plugin_registrant.dart
    - lib/generated**/*.dart
    #    - "**/i18n/**.g.dart"
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "test/**_test.mocks.dart"
    - "integration_test/**_test.mocks.dart"
    #    - "**/*.config.dart"
    - "**/*.generated.dart"
    # Build
    - 'build/**'
    # Assets
    - 'assets/**'
  #    - 'test/**'
  #    - 'integration_test/**'

  #  strong-mode:
  #    implicit-casts: true # allow resolve dynamic automatically
  #    implicit-dynamic: true # allow using Future.delayed(...), instead Future<void>.delayed(...)

  errors:
    always_use_package_imports: error
    avoid_relative_lib_imports: error
    close_sinks: error
    collection_methods_unrelated_type: warning
    lines_longer_than_80_chars: ignore # prefer using 100 chars per line (https://github.com/dart-lang/dart_style/issues/833)
    missing_required_param: error
    missing_return: error
    record_literal_one_positional_no_trailing_comma: error
    unrelated_type_equality_checks: warning
    one_member_abstracts: ignore # prefer using abstract classes with one member

linter:
  rules:
    sort_pub_dependencies: false
