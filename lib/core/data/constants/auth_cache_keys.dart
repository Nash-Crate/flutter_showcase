/// This class defines the keys used for caching authentication-related data
abstract class AuthCacheKeys {
  /// Key for storing the user session in cache
  static const String userSession = 'auth_user_session';

  /// Key for storing the user profile ID in cache
  static const String userProfileId = 'auth_user_profile_id';
}
